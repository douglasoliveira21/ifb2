ENVIRONMENT=development
DATABASE_URL=postgresql+psycopg://ifb:ifb@db:5432/ifb
CORS_ORIGINS=http://localhost:3000
ADMIN_USERNAME=admin
ADMIN_PASSWORD=troque-esta-senha

# Necessária apenas para a varredura de Frases Verificadas
# (app/sync/claim_scan.py, disparada via POST /api/admin/claim-scan por um
# agendador externo — ver README). Obtenha em
# https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=
