ENVIRONMENT=development
DATABASE_URL=postgresql+psycopg://ifb:ifb@db:5432/ifb
CORS_ORIGINS=http://localhost:3000
ADMIN_USERNAME=admin
ADMIN_PASSWORD=troque-esta-senha

# Necessária apenas para o serviço Cron de varredura de Frases Verificadas
# (app/sync/claim_scan.py) — não é usada pelo serviço web nem pelo sync
# principal. Obtenha em https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
